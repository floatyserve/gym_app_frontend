export interface UpdateWorkerRequest {
    email: string;
    role: string;
    active: boolean;
    firstName: string;
    lastName: string;
    phoneNumber: string;
    birthDate: string;
    hiredAt: string;
}